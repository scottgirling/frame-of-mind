"use client";
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#6750A4",
      light: "#EADDFF",
      dark: "#4F378B",
      contrastText: "#FFFFFF",
    },
  },
});

export default theme;
