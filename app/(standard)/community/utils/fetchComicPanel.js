import { db } from "@/lib/firebase";
import { doc, getDoc } from "firebase/firestore";

export default async function getPanelImage() {
  try {
  } catch (error) {
    console.error("error fetching panel", error);
  }
}
